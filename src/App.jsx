export default function App() {
  return (
    <main
      style={{
        minHeight: "100vh",
        display: "grid",
        placeItems: "center",
        fontFamily: "Segoe UI, sans-serif",
        background: "linear-gradient(120deg, #f6f9fc 0%, #e6eef8 100%)"
      }}
    >
      <h1 style={{ fontSize: "clamp(2rem, 8vw, 4rem)", margin: 0 }}>FluxoX</h1>
    </main>
  );
}